# central_system.py
def register_with_security_system(car_id, public_key):
    central_system = {}  # This is a mockup. In a real system, a database or a more complex backend would store this.
    central_system[car_id] = public_key
    return "Car registered successfully with central system!"
